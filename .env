VITE_PUBLIC_SERVER=changeme
JWT_SECRET=changeme

# Path to app data (parquet files, etc.) - mounted in Docker as /app_data
APP_DATA_PATH=changeme

# DuckDB file path for native queries
DUCKDB_PATH=changeme
SEARCH_INDEX_PATH=changeme
